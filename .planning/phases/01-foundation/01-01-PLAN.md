---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/006_paper_sports_bets.sql
  - api/settle-sports-bets.js
  - src/lib/initNewUser.js
autonomous: true
requirements:
  - INTG-02

must_haves:
  truths:
    - "paper_sports_bets table has columns: status, result_resolved_at, actual_payout, parlay_id, potential_payout"
    - "RLS is enabled on paper_sports_bets — authenticated users can only query their own rows"
    - "The settle-sports-bets cron writes result_resolved_at (not settled_at) when resolving bets"
    - "New users get a paper_sports_bankroll row on initialization so bet placement does not silently fail"
  artifacts:
    - path: "supabase/migrations/006_paper_sports_bets.sql"
      provides: "Schema migration: missing columns + RLS policy"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "api/settle-sports-bets.js"
      provides: "Cron writes result_resolved_at instead of settled_at"
      contains: "result_resolved_at"
    - path: "src/lib/initNewUser.js"
      provides: "Bankroll row initialization for new users"
      contains: "paper_sports_bankroll"
  key_links:
    - from: "api/settle-sports-bets.js"
      to: "paper_sports_bets.result_resolved_at"
      via: "supabase .update() call"
      pattern: "result_resolved_at.*toISOString"
    - from: "src/lib/initNewUser.js"
      to: "paper_sports_bankroll"
      via: "supabase .upsert() call"
      pattern: "paper_sports_bankroll"
---

<objective>
Verify and fix the paper_sports_bets schema, enable RLS, update the cron to write result_resolved_at, and ensure new users get a bankroll row.

Purpose: The data layer must be correct and secure before Phase 2 builds any UI that reads from paper_sports_bets. Missing columns, absent RLS, and uninitialized bankroll rows would cause silent failures or data leaks.

Output: Migration SQL file, updated cron endpoint, updated initNewUser.js
</objective>

<execution_context>
@/Users/stratify/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stratify/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing cron update call in api/settle-sports-bets.js (line 120-129): -->
```javascript
await supabase
  .from('paper_sports_bets')
  .update({
    status: result,
    actual_payout: actualPayout,
    home_score: homeScore,
    away_score: awayScore,
    settled_at: new Date().toISOString(),
  })
  .eq('id', bet.id);
```

<!-- Existing initNewUser.js structure (line 42-48): -->
```javascript
const { error: upsertError } = await supabase
  .from('profiles')
  .upsert(defaults, { onConflict: 'id' });

if (upsertError) {
  throw upsertError;
}
```

<!-- Existing migration files in supabase/migrations/: -->
<!-- 001 through 005 exist. Next file is 006. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema migration and enable RLS</name>
  <files>supabase/migrations/006_paper_sports_bets.sql</files>
  <action>
Create `supabase/migrations/006_paper_sports_bets.sql` with the following SQL:

1. Add missing columns using `ADD COLUMN IF NOT EXISTS` (idempotent — safe if columns already exist):
   - `status text DEFAULT 'pending'`
   - `result_resolved_at timestamptz`
   - `actual_payout numeric`
   - `potential_payout numeric`
   - `parlay_id uuid`

2. Enable Row Level Security:
   - `ALTER TABLE paper_sports_bets ENABLE ROW LEVEL SECURITY;`

3. Create user-scoped RLS policy:
   - `DROP POLICY IF EXISTS "Users see own bets" ON paper_sports_bets;`
   - `CREATE POLICY "Users see own bets" ON paper_sports_bets FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);`

4. Add a composite index for Phase 2 history queries:
   - `CREATE INDEX IF NOT EXISTS idx_paper_sports_bets_user_created ON paper_sports_bets (user_id, created_at DESC);`

Note: The service-role key used by the settle-sports-bets cron bypasses RLS by design — this is correct and intentional (confirmed in research).
  </action>
  <verify>
    <automated>cat supabase/migrations/006_paper_sports_bets.sql && echo "---FILE EXISTS---"</automated>
  </verify>
  <done>Migration file exists with all 5 ADD COLUMN IF NOT EXISTS statements, ENABLE ROW LEVEL SECURITY, RLS policy creation, and composite index. File is ready to run against Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Update cron to write result_resolved_at and add bankroll init</name>
  <files>api/settle-sports-bets.js, src/lib/initNewUser.js</files>
  <action>
**In api/settle-sports-bets.js:**
Find the `.update()` call around line 122-128. Replace `settled_at: new Date().toISOString()` with `result_resolved_at: new Date().toISOString()`. Do NOT change any other field in the update object. Do NOT rename the variable or restructure the code. Surgical change only.

**In src/lib/initNewUser.js:**
After the profiles upsert succeeds (after line 48, before the return statement on line 50), add a paper_sports_bankroll upsert:

```javascript
// Initialize sportsbook bankroll for new users
await supabase
  .from('paper_sports_bankroll')
  .upsert(
    { user_id: userId, balance: 100000 },
    { onConflict: 'user_id', ignoreDuplicates: true }
  );
```

Use `ignoreDuplicates: true` so existing users are not affected. Do NOT modify any other code in initNewUser.js. Do NOT change the MAG7_WATCHLIST, the profiles upsert, or the function signature.
  </action>
  <verify>
    <automated>grep -n "result_resolved_at" api/settle-sports-bets.js && grep -n "paper_sports_bankroll" src/lib/initNewUser.js && echo "---BOTH UPDATED---"</automated>
  </verify>
  <done>settle-sports-bets.js writes result_resolved_at instead of settled_at. initNewUser.js upserts a paper_sports_bankroll row with balance 100000 for new users. No other code in either file was changed.</done>
</task>

</tasks>

<verification>
1. `cat supabase/migrations/006_paper_sports_bets.sql` — contains ADD COLUMN IF NOT EXISTS for all 5 columns, ENABLE ROW LEVEL SECURITY, RLS policy, and composite index
2. `grep "result_resolved_at" api/settle-sports-bets.js` — returns the updated line
3. `grep "settled_at" api/settle-sports-bets.js` — returns NO matches (old column name removed)
4. `grep "paper_sports_bankroll" src/lib/initNewUser.js` — returns the upsert call
5. `npm run dev` — Vite starts without circular import warnings or errors
</verification>

<success_criteria>
- Migration file 006_paper_sports_bets.sql exists with idempotent column additions, RLS enablement, and user-scoped policy
- settle-sports-bets.js writes result_resolved_at (not settled_at) when resolving bets
- initNewUser.js upserts a paper_sports_bankroll row for new users
- Vite dev server starts cleanly with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
