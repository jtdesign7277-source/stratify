---
phase: 02-bet-history-display
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/dashboard/BetHistorySummary.jsx
  - src/components/dashboard/BetHistoryTab.jsx
autonomous: false
requirements:
  - HIST-01
  - HIST-03
  - HIST-04
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "User sees a stat strip with total wagered, total won, net P&L, and win rate"
    - "User sees a table of all bets with date/time, matchup, sport, stake, odds, payout, result columns"
    - "Won bets display in emerald text, lost in red, pending in gray — plain text only"
    - "User with no bets sees empty state with guidance to place first bet"
    - "All components use glass card styling and monospace for numbers"
  artifacts:
    - path: "src/components/dashboard/BetHistorySummary.jsx"
      provides: "Stat strip with 4 P&L metrics"
      min_lines: 40
    - path: "src/components/dashboard/BetHistoryTab.jsx"
      provides: "Bet history table with empty state"
      min_lines: 80
  key_links:
    - from: "src/components/dashboard/BetHistorySummary.jsx"
      to: "src/lib/sportsUtils.js"
      via: "import GLASS_CARD, DESIGN_COLORS"
      pattern: "import.*sportsUtils"
    - from: "src/components/dashboard/BetHistoryTab.jsx"
      to: "src/lib/sportsUtils.js"
      via: "import GLASS_CARD, DESIGN_COLORS"
      pattern: "import.*sportsUtils"
    - from: "src/components/dashboard/BetHistoryTab.jsx"
      to: "src/hooks/useBetHistory.js"
      via: "receives bets as props (not direct import)"
      pattern: "bets"
---

<objective>
Build the BetHistorySummary stat strip and BetHistoryTab table component that display bet history data.

Purpose: These are the visible UI that users interact with. BetHistorySummary shows aggregate P&L; BetHistoryTab shows the full bet log with color-coded results and an empty state for new users.
Output: Two new JSX components ready for Phase 3 tab integration.
</objective>

<execution_context>
@/Users/stratify/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stratify/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-bet-history-display/02-CONTEXT.md
@.planning/phases/02-bet-history-display/02-RESEARCH.md
@.planning/phases/02-bet-history-display/02-01-SUMMARY.md

@src/lib/sportsUtils.js
@src/hooks/useBetHistory.js
@src/components/dashboard/SportsBankroll.jsx

<interfaces>
<!-- From Plan 02-01 output -->
From src/hooks/useBetHistory.js:
```javascript
export function useBetHistory(): { bets: Array, loading: boolean, error: string|null }
export function computeStats(bets: Array): { totalWagered: number, totalWon: number, netPnl: number, winRate: number }
```

From src/lib/sportsUtils.js:
```javascript
export const GLASS_CARD = {
  standard: 'bg-gradient-to-br from-white/[0.04] to-white/[0.01] backdrop-blur-xl rounded-2xl border border-white/[0.06] shadow-[...]',
  // ... other variants
};
export const DESIGN_COLORS = { /* emerald, red, gray tokens */ };
export function calcPayout(stake, odds): number;
```

<!-- Design system tokens (LOCKED) -->
Background: bg-[#0a0a0f]
Win: text-emerald-400
Loss: text-red-400
Pending: text-gray-400
Numbers: font-mono font-medium
Headers: font-semibold tracking-tight
Section labels: text-xs font-semibold tracking-widest text-gray-500 uppercase
No badges/pills — plain colored text only
No icon boxes — bare SVG only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BetHistorySummary and BetHistoryTab components</name>
  <files>src/components/dashboard/BetHistorySummary.jsx, src/components/dashboard/BetHistoryTab.jsx</files>
  <action>
**BetHistorySummary.jsx:**
- Props: { stats } where stats = { totalWagered, totalWon, netPnl, winRate } from computeStats()
- Import GLASS_CARD, DESIGN_COLORS from '../../lib/sportsUtils'
- Import CountUp from 'react-countup'
- Import { motion } from 'framer-motion'
- Render a horizontal stat strip inside a GLASS_CARD.standard container
- 4 stat cells in a row (grid or flex): Total Wagered, Total Won, Net P&L, Win Rate
- Each cell: section label (text-xs font-semibold tracking-widest text-gray-500 uppercase) + value below
- Values use CountUp with duration=0.8, separator=",", decimals=2 (except winRate: decimals=1 + "%" suffix)
- Net P&L value: text-emerald-400 if >= 0, text-red-400 if < 0
- All numeric values: font-mono font-medium
- Dollar sign prefix on monetary values
- Animate container entrance with framer-motion: initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ type: 'spring', stiffness: 400, damping: 30 }}

**BetHistoryTab.jsx:**
- Props: { bets, loading, error, stats } — receives data from useBetHistory() called in parent
- Import GLASS_CARD from '../../lib/sportsUtils'
- Import BetHistorySummary from './BetHistorySummary'
- Import { motion, AnimatePresence } from 'framer-motion'
- Import computeStats from '../../hooks/useBetHistory' (to compute stats if not passed, but prefer props)

**Layout:**
1. BetHistorySummary stat strip at top (pass stats prop)
2. Table below in a GLASS_CARD.standard container

**Table columns** (use exact column names from 02-01-SUMMARY.md):
- Date/Time: format created_at as "MMM DD, YYYY HH:MM" (use toLocaleDateString/toLocaleTimeString or Intl.DateTimeFormat)
- Matchup: display the matchup/teams column value (use column name verified in Plan 01)
- Sport: sport column, uppercase
- Stake: bet_amount, font-mono, formatted as $X.XX
- Odds: odds column, font-mono, prefix with "+" if positive
- Payout: actual_payout for settled bets, potential_payout for pending bets, font-mono, formatted as $X.XX
- Result: status column mapped to display text — "Won"/"Lost"/"Pending"

**Result coloring (LOCKED — no badges/pills):**
```
const RESULT_COLOR = { won: 'text-emerald-400', lost: 'text-red-400', pending: 'text-gray-400' };
```
Result text: `<span className={`${RESULT_COLOR[bet.status]} text-sm font-medium`}>{label}</span>`
WRONG: Any background color, rounded-full, px/py padding on result text

**Row animation:**
- motion.tbody with variants={{ show: { transition: { staggerChildren: 0.04 } } }}
- motion.tr with variants={{ hidden: { opacity: 0, y: 6 }, show: { opacity: 1, y: 0 } }}
- transition={{ type: 'spring', stiffness: 400, damping: 30 }}
- whileHover={{ backgroundColor: 'rgba(255,255,255,0.04)' }}

**Loading state:** Simple centered spinner or pulsing text "Loading bet history..." in text-gray-500

**Error state:** Red text with error message

**Empty state (HIST-04):**
When bets.length === 0 and !loading:
```jsx
<div className="flex flex-col items-center justify-center py-16 text-center">
  <span className="mb-3 text-3xl opacity-40">&#x1f4cb;</span>
  <p className="text-sm font-medium text-gray-400">No bets yet</p>
  <p className="mt-1 text-xs text-gray-500">Place your first paper bet to start tracking performance</p>
</div>
```
Do NOT render a blank table or table headers when empty.

**Table header styling:** bg-black/20 text, text-xs font-semibold tracking-widest text-gray-500 uppercase for th elements, sticky if possible.

**Background:** bg-[#0a0a0f] on outermost container.
  </action>
  <verify>
    <automated>cd "/Users/stratify/Library/Mobile Documents/com~apple~CloudDocs/Desktop/Stratify" && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
- BetHistorySummary renders 4 stat cells with CountUp animated values
- BetHistoryTab renders table with 7 columns matching HIST-01
- Won=emerald, Lost=red, Pending=gray as plain text (HIST-03)
- Empty state renders guidance when bets is empty (HIST-04)
- All glass card styling imported from sportsUtils.js
- All numbers use font-mono font-medium
- No badges, pills, icon boxes anywhere
- Vite dev server starts without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete bet history data layer (useBetHistory hook) and UI components (BetHistorySummary stat strip + BetHistoryTab table) with glass card styling, framer-motion animations, CountUp stats, and color-coded results.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:5173
2. To test the components, temporarily import and render BetHistoryTab in a visible location (or the executor may have created a test route)
3. Verify:
   - If you have bets: table shows all 7 columns, numbers are monospace, won=emerald/lost=red/pending=gray as plain text (no badges)
   - If no bets: empty state shows clipboard icon + "No bets yet" + guidance text
   - Stat strip shows Total Wagered, Total Won, Net P&L, Win Rate with animated count-up
   - Glass card styling matches rest of dashboard (gradient, blur, rounded, border)
   - Row hover shows subtle white highlight
   - Rows animate in with stagger effect
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- npm run dev starts without errors
- BetHistorySummary.jsx exists with CountUp stats and GLASS_CARD import
- BetHistoryTab.jsx exists with table, result coloring, empty state
- No badge/pill styling anywhere (grep for rounded-full in these files should return nothing)
- GLASS_CARD imported from sportsUtils.js (not copy-pasted)
- All numeric values use font-mono
</verification>

<success_criteria>
- Table displays all 7 HIST-01 columns with correct data
- Results color-coded per HIST-03 (emerald/red/gray, plain text)
- Empty state renders per HIST-04
- Stat strip shows 4 metrics per STAT-01
- Stats computed from bets array prop enabling STAT-02 filter support
- Visual design matches Stratify dashboard (glass, dark bg, monospace numbers)
</success_criteria>

<output>
After completion, create `.planning/phases/02-bet-history-display/02-02-SUMMARY.md`
</output>
